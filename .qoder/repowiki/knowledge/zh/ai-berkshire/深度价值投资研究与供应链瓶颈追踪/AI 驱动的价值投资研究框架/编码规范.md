- 所有财务指标计算必须调用 `tools/financial_rigor.py` 使用 Decimal 精确计算，禁止 LLM 心算
- 关键财务数据必须遵循 `skills/financial-data.md` 规范，至少从两个独立来源获取并交叉验证
- 研究报告输出前必须执行 `tools/report_audit.py` 进行随机数据抽检，偏差超过 1% 需打回修正
- 所有 Skill 执行时必须进行日期锚定（$CURRENT_DATE），确保数据时效性与搜索关键词包含当前年份