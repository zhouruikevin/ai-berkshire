- **精确十进制计算**：`financial_rigor.py` 使用 `decimal.Decimal` 引擎避免浮点数漂移，支持 Benford 定律检测与多源交叉验证。
- **浏览器自动化**：`xueqiu_scraper.py` 依赖 `playwright` 进行 Headful/Headless 浏览器控制与状态管理。
- **数据交互**：广泛使用 `curl` 子进程调用（如 `morningstar_fair_value.py`, `stock_screener.py`）绕过 Python SSL 限制获取金融 API 数据。