- 所有核心计算脚本均采用 `argparse` 构建子命令（sub-parsers）驱动的 CLI 接口，支持复杂的参数组合与帮助文档输出。
- 金融数值处理统一采用精确十进制（`Decimal`）或明确的浮点容差比对（如 `report_audit.py` 中的 1% 偏差阈值），杜绝直接浮点相等判断。
- 数据获取层普遍采用“本地缓存/手工录入优先，API 实时获取为辅”的策略，基本面数据多以 `OrderedDict` 或 JSON 文件形式硬编码在脚本中以保证回测稳定性。